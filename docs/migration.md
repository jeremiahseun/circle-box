# Naming Migration Notes

This repository uses `CircleBox` naming.

Historical references to earlier naming are intentionally documented here only for migration context. CI excludes this file from naming checks.
