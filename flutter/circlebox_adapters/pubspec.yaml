name: circlebox_adapters
description: Companion adapters for forwarding CircleBox exports into Sentry/PostHog pipelines.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  circlebox_flutter:
    path: ../circlebox_flutter

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
